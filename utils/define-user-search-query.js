const defineUserSearchQuery = (req) => {
  return req.query.search
    ? {
        $or: [
          { name: { $regex: req.query.search, $options: 'i' } },
          { email: { $regex: req.query.search, $options: 'i' } },
        ],
      }
    : {};
};

module.exports = defineUserSearchQuery;
